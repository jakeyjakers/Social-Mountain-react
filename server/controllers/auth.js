module.exports = {
    login: (req, res) => {
        console.log( `this is the login in auth.js`)
    },
    register: (req, res) => {
        console.log( `this is the register in auth.js`)
    }
    
}